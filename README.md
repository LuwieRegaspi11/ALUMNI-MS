
  # Alumni Tracer and Donation System
  
  - Tech stack table (React, Vite, Tailwind, shadcn/ui, MUI, Recharts, etc.)
  - Current state warning — clearly flags that everything is mock/in-memory data, including the demo login credentials table 
  (admin/alumni/rep/faculty   accounts + passwords)
  - Project structure — full folder breakdown with what lives where
  - Roles and routes — every route per role, plus a note about the two orphaned unused pages (LoginPage.tsx, RegistrationPage.tsx)
  - Key features already implemented — dark mode mechanics, responsive behavior, role-based access
  - Known limitations / next steps — no backend yet, remaining remote avatar images, no tests
  

  ## Running the code
  
  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

 
